import SwiftUI

// TODO (Session 3): Implement camera profile selection (ARRI / RED / Sony).
struct CameraSelectionView: View {
    var body: some View {
        Text("Kamera-Auswahl — kommt in Session 3")
            .foregroundStyle(.white)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .background(Color.black)
    }
}
